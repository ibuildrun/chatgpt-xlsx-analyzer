version: '3.8'

services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3003:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - ./data:/app/data
    command: bun run dev
